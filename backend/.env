# ── MedGemma LLM (via llama.cpp) ──
# Start: llama-server -m <medgemma.gguf> --port 8080
LLM_BASE_URL=http://localhost:8080

# ── EmbeddingGemma (via llama.cpp --embedding) ──
# Start: llama-server -m <embedding-gemma.gguf> --port 8081 --embedding
EMBEDDING_BASE_URL=http://localhost:8081

# Server settings
HOST=0.0.0.0
PORT=8000
